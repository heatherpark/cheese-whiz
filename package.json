{
  "name": "mvp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "~4.0.0",
    "nodemon": "^1.2.1"
  },
  "scripts": {
    "start": "./node_modules/.bin/nodemon --ignore node_modules index.js"
  }
}
